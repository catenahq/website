# catenahq/website -- standalone .gitignore.

# Node + Astro build artifacts.
node_modules/
dist/
.astro/
*.tsbuildinfo

# Local env files (Astro / Vite convention).
.env
.env.local
.env.*.local

# Editor and OS noise.
.DS_Store
.idea/
.vscode/
.~lock*

# token-savior MCP per-project index cache.
.token-savior-cache.json
